# Provider API Keys (Required based on provider choice)
# ANTHROPIC_API_KEY=your-anthropic-api-key-here
# OPENAI_API_KEY=your-openai-api-key-here
# OLLAMA_BASE_URL=http://localhost:11434

# Main AI Configuration (Required)
# AI_PROVIDER=choose between anthropic, openai, ollama
# AI_MODEL=choose modal

# Intent Classification (Optional: keyword matching falls back to LLM if confidence is low and LLM is enabled.)
# INTENT_PROVIDER=choose between anthropic, openai, ollama
# INTENT_MODEL=choose modal

# Optional: Set to 'production' to hide detailed errors
# NODE_ENV=development
