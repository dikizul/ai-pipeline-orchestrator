# Provider API Keys (Required based on provider choice)
# ANTHROPIC_API_KEY=your-anthropic-api-key-here
# OPENAI_API_KEY=your-openai-api-key-here
# DEEPSEEK_API_KEY=your-deepseek-api-key-here (for cloud API only)
# OLLAMA_BASE_URL=http://localhost:11434

# Main AI Chat Configuration (Required)
# AI_PROVIDER=choose between anthropic, openai, deepseek, ollama
# AI_MODEL=choose modal

# Intent Classification (Optional: keyword matching falls back to LLM if confidence is low and LLM is enabled.)
# INTENT_PROVIDER=choose between anthropic, openai, deepseek, ollama
# INTENT_MODEL=choose modal

# Examples:
# Local DeepSeek (via Ollama - FREE):
#   AI_PROVIDER=ollama
#   AI_MODEL=deepseek-r1
#
# Cloud DeepSeek (requires API key):
#   AI_PROVIDER=deepseek
#   AI_MODEL=deepseek-chat
#   DEEPSEEK_API_KEY=your-key

# Optional: Set to 'production' to hide detailed errors
# NODE_ENV=development
